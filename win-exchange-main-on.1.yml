---
- name: Enable Exchange Maintenance Mode
  hosts: "{{ hosts }}"
  tasks:
    - name: Enable Exchange Maintenance Mode
      win_shell: |
        Add-PSSnapin Microsoft.Exchange.Management.PowerShell.SnapIn
        Get-ServerComponentState -Identity exch01.exch.arrowdemo.dk
        become: yes
        become_user: Administrator